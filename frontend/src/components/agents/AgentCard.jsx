/**
 * File path: frontend/src/components/agents/AgentCard.jsx
 * Purpose: Card for a single agent showing: ID, name, category, current signal badge, score, confidence, last run time, health status.
 * Props: agent, onClick
 * TODO: Implement component UI and wire to API.
 */
'use client';
import React from 'react';
export default function AgentCard({ agent, onClick }) {
  return (
    <div className="rounded-lg border border-border bg-card p-4">
      <p className="text-muted-foreground text-sm">AgentCard — not yet implemented</p>
    </div>
  );
}
