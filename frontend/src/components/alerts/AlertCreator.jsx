/**
 * File path: frontend/src/components/alerts/AlertCreator.jsx
 * Purpose: Form/modal for creating a new alert: symbol picker, alert type selector, threshold input, notification prefs.
 * Props: isOpen, onClose, onSubmit, defaultSymbol
 * TODO: Implement component UI and wire to API.
 */
'use client';
import React from 'react';
export default function AlertCreator({ isOpen, onClose, onSubmit, defaultSymbol }) {
  return (
    <div className="rounded-lg border border-border bg-card p-4">
      <p className="text-muted-foreground text-sm">AlertCreator — not yet implemented</p>
    </div>
  );
}
